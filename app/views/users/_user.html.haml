%h2.nickname
  %span= user.nickname
  = link_to '', github_url(user.nickname), target: '_blank', class: 'octicon octicon-mark-github'
- if user.last_activity_at
  %p Ostatnia aktywność: #{I18n.l(user.last_activity_at, :format => '%d %b %Y, %H:%M')}
