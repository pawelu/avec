.container
  %nav
    - if @current_user
      .span6
        %h2
          %p Profil
        %h2
          %p Imię:
          = @current_user.nickname
        %h2
          %p Email:
          = @current_user.email
        %h2= link_to 'Ustawienia', settings_path, class: 'btn btn-primary'

